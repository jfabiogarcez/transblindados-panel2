# transblindados-panel2
echo "# transblindados-panel" >> README.md git init git add README.md git commit -m "first commit" git branch -M main git remote add origin https://github.com/jfabiogarcez/transblindados-panel.git git push -u origin main
